<Window x:Class="WpfIveco.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Iveco Green Ledger" Height="720" Width="1150"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        FontFamily="Segoe UI Variable, Segoe UI, Helvetica Neue, sans-serif">

    <Window.Resources>
        <SolidColorBrush x:Key="OledBlack" Color="#000000"/>
        <SolidColorBrush x:Key="ElevatedGray1" Color="#1C1C1E"/>
        <SolidColorBrush x:Key="ElevatedGray2" Color="#2C2C2E"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#8E8E93"/>
        <SolidColorBrush x:Key="AppleBlue" Color="#0A84FF"/>
        <SolidColorBrush x:Key="AppleGreen" Color="#30D158"/>

        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PremiumCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource ElevatedGray1}"/>
            <Setter Property="CornerRadius" Value="24"/>
            <Setter Property="BorderBrush" Value="#3A3A3C"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource OledBlack}" CornerRadius="30" BorderBrush="#2C2C2E" BorderThickness="1" Margin="20">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="Transparent" Padding="20,24,20,24">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Top" Margin="8,0,0,30">
                        <Button Width="12" Height="12" Margin="0,0,8,0" Cursor="Hand" Click="CloseButton_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Ellipse Fill="#FF5F56"/>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button Width="12" Height="12" Margin="0,0,8,0" Cursor="Hand" Click="MinimizeButton_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Ellipse Fill="#FFBD2E"/>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button Width="12" Height="12" Cursor="Hand" Click="MaximizeButton_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Ellipse Fill="#27C93F"/>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Margin="0,20,0,0">
                        <TextBlock Text="MENU PRINCIPAL" Foreground="{StaticResource TextSecondary}" FontSize="11" FontWeight="SemiBold" Margin="14,0,0,16" Opacity="0.7"/>

                        <Button Command="{Binding MudarAbaCommand}" CommandParameter="Dashboard">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource MenuButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding AbaAtiva}" Value="Dashboard">
                                            <Setter Property="Background" Value="{StaticResource ElevatedGray2}"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE80F;" FontSize="15" Margin="0,0,14,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Dashboard" FontSize="14.5" VerticalAlignment="Center" FontWeight="Medium"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding MudarAbaCommand}" CommandParameter="Projetos">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource MenuButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding AbaAtiva}" Value="Projetos">
                                            <Setter Property="Background" Value="{StaticResource ElevatedGray2}"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE70F;" FontSize="15" Margin="0,0,14,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Chão de Fábrica" FontSize="14.5" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding MudarAbaCommand}" CommandParameter="Cadastro">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource MenuButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding AbaAtiva}" Value="Cadastro">
                                            <Setter Property="Background" Value="{StaticResource ElevatedGray2}"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE710;" FontSize="15" Margin="0,0,14,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Cadastro" FontSize="14.5" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding MudarAbaCommand}" CommandParameter="Analises">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource MenuButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding AbaAtiva}" Value="Analises">
                                            <Setter Property="Background" Value="{StaticResource ElevatedGray2}"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE9D9;" FontSize="15" Margin="0,0,14,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Análises" FontSize="14.5" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>

                        <Button Command="{Binding MudarAbaCommand}" CommandParameter="Ajustes">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource MenuButtonStyle}">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding AbaAtiva}" Value="Ajustes">
                                            <Setter Property="Background" Value="{StaticResource ElevatedGray2}"/>
                                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE713;" FontSize="15" Margin="0,0,14,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Ajustes" FontSize="14.5" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Column="1" Background="Transparent" Margin="20,30,40,40">

                <Grid>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding AbaAtiva}" Value="Dashboard">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Overview API" Foreground="{StaticResource TextPrimary}" FontSize="34" FontWeight="SemiBold" Margin="10,0,0,40"/>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="180"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource PremiumCardStyle}" Margin="0,0,12,24">
                            <StackPanel Margin="30">
                                <TextBlock Text="Total de Veículos" Foreground="{StaticResource TextSecondary}" FontSize="14" FontWeight="Medium"/>
                                <TextBlock Text="{Binding TotalVeiculos}" Foreground="{StaticResource TextPrimary}" FontSize="42" FontWeight="SemiBold" Margin="0,6,0,0"/>
                                <Border Background="#1A30D158" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left" Margin="0,12,0,0">
                                    <TextBlock Text="Ao Vivo do Firebase" Foreground="{StaticResource AppleGreen}" FontSize="12.5" FontWeight="SemiBold"/>
                                </Border>
                            </StackPanel>
                        </Border>

                        <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource PremiumCardStyle}" Margin="12,0,0,24">
                            <StackPanel Margin="30">
                                <TextBlock Text="Fornecedores Ativos" Foreground="{StaticResource TextSecondary}" FontSize="14" FontWeight="Medium"/>
                                <TextBlock Text="{Binding TotalFornecedores}" Foreground="{StaticResource TextPrimary}" FontSize="42" FontWeight="SemiBold" Margin="0,6,0,0"/>
                                <Border Background="#1A0A84FF" CornerRadius="6" Padding="8,4" HorizontalAlignment="Left" Margin="0,12,0,0">
                                    <TextBlock Text="Ao Vivo do Firebase" Foreground="{StaticResource AppleBlue}" FontSize="12.5" FontWeight="SemiBold"/>
                                </Border>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>

                <Grid>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding AbaAtiva}" Value="Projetos">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Rastreabilidade de Componentes" Foreground="{StaticResource TextPrimary}" FontSize="34" FontWeight="SemiBold" Margin="0,0,0,30"/>

                    <Border Grid.Row="1" Style="{StaticResource PremiumCardStyle}" Padding="24" VerticalAlignment="Top">
                        <StackPanel>
                            <TextBlock Text="Pesquisa de Pegada de Carbono de Veículo" Foreground="{StaticResource TextPrimary}" FontSize="18" FontWeight="Medium" Margin="0,0,0,16"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="{StaticResource OledBlack}" CornerRadius="12" Padding="12,8" BorderThickness="1" BorderBrush="{StaticResource ElevatedGray2}" Margin="0,0,16,0">
                                    <Grid>
                                        <TextBlock Text="Insira o número VIN do veículo..." Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="5,0,0,0" IsHitTestVisible="False">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding PesquisaVin}" Value="">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <TextBox Text="{Binding PesquisaVin, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextPrimary}" FontSize="14" VerticalAlignment="Center" CaretBrush="{StaticResource TextPrimary}" Margin="5,0,0,0"/>
                                    </Grid>
                                </Border>
                                <Button Grid.Column="1" Command="{Binding PesquisarVinCommand}" Padding="25,10" Background="{StaticResource AppleBlue}" Foreground="White" BorderThickness="0" Cursor="Hand">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                                <TextBlock Text="Rastrear" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>

                <Grid>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding AbaAtiva}" Value="Cadastro">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Gestão de Fornecedores" Foreground="{StaticResource TextPrimary}" FontSize="34" FontWeight="SemiBold" Margin="0,0,0,30"/>

                    <StackPanel Grid.Row="1">
                        <Border Style="{StaticResource PremiumCardStyle}" Padding="24">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="Registar Novo Fornecedor (Via Receita Federal)" Foreground="{StaticResource TextPrimary}" FontSize="18" FontWeight="Medium" Margin="0,0,0,20"/>

                                <Grid Grid.Row="1" Margin="0,0,0,20">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" Background="{StaticResource OledBlack}" CornerRadius="12" Padding="12,8" BorderThickness="1" BorderBrush="{StaticResource ElevatedGray2}" Margin="0,0,16,0">
                                        <Grid>
                                            <TextBlock Text="Digite o CNPJ do Fornecedor (apenas números)" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="5,0,0,0" IsHitTestVisible="False">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding CnpjBusca}" Value="">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                            <TextBox Text="{Binding CnpjBusca, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextPrimary}" FontSize="14" VerticalAlignment="Center" CaretBrush="{StaticResource TextPrimary}" Margin="5,0,0,0"/>
                                        </Grid>
                                    </Border>

                                    <Button Grid.Column="1" Command="{Binding ConsultarCnpjCommand}" Padding="25,10" Background="{StaticResource AppleBlue}" Foreground="{StaticResource TextPrimary}" BorderThickness="0" Cursor="Hand">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                                    <TextBlock Text="Consultar CNPJ" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>

                                <StackPanel Grid.Row="2" Margin="0,0,0,24">
                                    <TextBlock Text="Nome Comercial / Razão Social" Foreground="{StaticResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                    <Border Background="{StaticResource OledBlack}" CornerRadius="8" Padding="12,10" BorderThickness="1" BorderBrush="#2C2C2E">
                                        <TextBlock Text="{Binding NomeFornecedorEncontrado}" Foreground="{StaticResource TextPrimary}" FontSize="15" MinHeight="20"/>
                                    </Border>

                                    <TextBlock Text="Localização Oficial Registada" Foreground="{StaticResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,12,0,4"/>
                                    <Border Background="{StaticResource OledBlack}" CornerRadius="8" Padding="12,10" BorderThickness="1" BorderBrush="#2C2C2E">
                                        <TextBlock Text="{Binding LocalizacaoFornecedorEncontrado}" Foreground="{StaticResource TextPrimary}" FontSize="15" MinHeight="20"/>
                                    </Border>
                                </StackPanel>

                                <Grid Grid.Row="3">
                                    <TextBlock Text="{Binding MensagemCadastro}" Foreground="{StaticResource TextSecondary}" FontSize="14" VerticalAlignment="Center" FontStyle="Italic"/>
                                    <Button Command="{Binding SalvarFornecedorCommand}" HorizontalAlignment="Right" Padding="30,12" Background="{StaticResource AppleGreen}" Foreground="White" BorderThickness="0" Cursor="Hand">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="12">
                                                    <TextBlock Text="Guardar no Firebase" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </Grid>
                            </Grid>
                        </Border>
                    </StackPanel>
                </Grid>

                <Grid>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding AbaAtiva}" Value="Analises">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Análise de Sustentabilidade" Foreground="{StaticResource TextPrimary}" FontSize="34" FontWeight="SemiBold" Margin="0,0,0,30"/>
                    <Border Grid.Row="1" Style="{StaticResource PremiumCardStyle}" Padding="30">
                        <StackPanel>
                            <TextBlock Text="Relatórios ESG &amp; Pegada Carbónica" Foreground="{StaticResource TextPrimary}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                            <TextBlock Text="Módulo de gráficos avançados em conformidade com as diretivas de sustentabilidade." Foreground="{StaticResource TextSecondary}" FontSize="14" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <Grid>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding AbaAtiva}" Value="Ajustes">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Configurações" Foreground="{StaticResource TextPrimary}" FontSize="34" FontWeight="SemiBold" Margin="0,0,0,30"/>

                    <Border Grid.Row="1" Style="{StaticResource PremiumCardStyle}" Padding="24" VerticalAlignment="Top">
                        <StackPanel>
                            <TextBlock Text="Endereço Base da API" Foreground="{StaticResource TextSecondary}" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6"/>
                            <Border Background="{StaticResource OledBlack}" CornerRadius="12" Padding="12,10" BorderThickness="1" BorderBrush="{StaticResource ElevatedGray2}" Margin="0,0,0,20">
                                <TextBox Text="{Binding ApiUrlConfig, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextPrimary}" FontSize="14"/>
                            </Border>

                            <Grid Margin="0,0,0,20">
                                <StackPanel>
                                    <TextBlock Text="Simulador de Chão de Fábrica (IoT)" Foreground="{StaticResource TextPrimary}" FontSize="15" FontWeight="Medium"/>
                                    <TextBlock Text="Geração contínua e automática de veículos de teste no barramento." Foreground="{StaticResource TextSecondary}" FontSize="12"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding StatusSimulador}" Foreground="{StaticResource TextSecondary}" Margin="0,0,12,0" VerticalAlignment="Center" FontSize="13"/>
                                    <Button Command="{Binding LigarDesligarSimuladorCommand}" Padding="16,8" Background="{StaticResource ElevatedGray2}" Foreground="White" BorderThickness="0" Cursor="Hand">
                                        <Button.Template>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                                    <TextBlock Text="Alternar" Margin="10,4" HorizontalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Button.Template>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Border>
                </Grid>

            </Grid>
        </Grid>
    </Border>
</Window>