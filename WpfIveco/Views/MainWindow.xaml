<Window x:Class="WpfIveco.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:WpfIveco.Views"
        Title="Iveco Green Ledger" Height="800" Width="1300"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType"
        FontFamily="Segoe UI Variable, Segoe UI, Helvetica Neue, sans-serif">
    <Grid>
        <!-- ========================================== -->
        <!-- Painel Principal (logado)                   -->
        <!-- ========================================== -->
        <Border Background="{StaticResource AppBackground}" CornerRadius="20" BorderBrush="#E5E5EA" BorderThickness="1" Margin="10">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoggedIn}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.1"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Sidebar -->
                <Border Grid.Column="0" Background="{StaticResource OledBlack}" CornerRadius="20,0,0,20" BorderBrush="{StaticResource CardBorder}" BorderThickness="0,0,1,0">
                    <Grid Margin="20,24,20,24">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Botões de janela -->
                        <StackPanel Grid.Row="0" Orientation="Horizontal" VerticalAlignment="Top" Margin="8,0,0,30">
                            <Button Width="30" Height="30" Margin="0,0,4,0" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="MinimizeButton_Click" ToolTip="Minimizar">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE921;" FontSize="12" Foreground="#A3A3A3" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Button>
                            <Button Width="30" Height="30" Margin="0,0,4,0" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="MaximizeButton_Click" ToolTip="Maximizar">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE922;" FontSize="12" Foreground="#A3A3A3" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Button>
                            <Button Width="30" Height="30" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="CloseButton_Click" ToolTip="Fechar">
                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8BB;" FontSize="12" Foreground="#FF5F56" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Button>
                        </StackPanel>

                        <!-- Logo -->
                        <StackPanel Grid.Row="1" Margin="10,0,0,30" Orientation="Horizontal">
                            <Border Width="45" Height="45" CornerRadius="10" Background="{StaticResource ElevatedGray2}">
                                <TextBlock FontFamily="Segoe UI Emoji" Text="🌿" FontSize="28" Foreground="{StaticResource AppleGreen}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                                <TextBlock Text="Iveco S.p.A" Foreground="{StaticResource TextPrimary}" FontWeight="Bold" FontSize="16"/>
                                <TextBlock Text="Green Ledger Node" Foreground="{StaticResource AppleGreen}" FontWeight="Medium" FontSize="11"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Menu -->
                        <StackPanel Grid.Row="2">
                            <TextBlock Text="NAVEGAÇÃO" Foreground="{StaticResource TextTertiary}" FontSize="11" FontWeight="Bold" Margin="14,0,0,10"/>

                            <Button Command="{Binding MudarAbaCommand}" CommandParameter="Dashboard">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource MenuButtonStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AbaAtiva}" Value="Dashboard">
                                                <Setter Property="Background" Value="{StaticResource ElevatedGray2}"/>
                                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE80F;" FontSize="16" Margin="0,0,16,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Dashboard" FontSize="15" VerticalAlignment="Center" FontWeight="Medium"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding MudarAbaCommand}" CommandParameter="Projetos">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource MenuButtonStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AbaAtiva}" Value="Projetos">
                                                <Setter Property="Background" Value="{StaticResource ElevatedGray2}"/>
                                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE70F;" FontSize="16" Margin="0,0,16,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Rastreabilidade" FontSize="15" VerticalAlignment="Center" FontWeight="Medium"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding MudarAbaCommand}" CommandParameter="Cadastro">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource MenuButtonStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AbaAtiva}" Value="Cadastro">
                                                <Setter Property="Background" Value="{StaticResource ElevatedGray2}"/>
                                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE710;" FontSize="16" Margin="0,0,16,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Fornecedores" FontSize="15" VerticalAlignment="Center" FontWeight="Medium"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding MudarAbaCommand}" CommandParameter="Analises">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource MenuButtonStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AbaAtiva}" Value="Analises">
                                                <Setter Property="Background" Value="{StaticResource ElevatedGray2}"/>
                                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE9D9;" FontSize="16" Margin="0,0,16,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Análises ESG" FontSize="15" VerticalAlignment="Center" FontWeight="Medium"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding MudarAbaCommand}" CommandParameter="Pecas">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource MenuButtonStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AbaAtiva}" Value="Pecas">
                                                <Setter Property="Background" Value="{StaticResource ElevatedGray2}"/>
                                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE74C;" FontSize="16" Margin="0,0,16,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Peças e Componentes" FontSize="15" VerticalAlignment="Center" FontWeight="Medium"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding MudarAbaCommand}" CommandParameter="Relatorios">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource MenuButtonStyle}">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AbaAtiva}" Value="Relatorios">
                                                <Setter Property="Background" Value="{StaticResource ElevatedGray2}"/>
                                                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8A5;" FontSize="16" Margin="0,0,16,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Relatórios PDF" FontSize="15" VerticalAlignment="Center" FontWeight="Medium"/>
                                </StackPanel>
                            </Button>

                            <!-- Seção Admin -->
                            <StackPanel>
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsAdmin}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                                <TextBlock Text="SISTEMA" Foreground="{StaticResource TextTertiary}" FontSize="11" FontWeight="Bold" Margin="14,30,0,10"/>
                                <Button Command="{Binding MudarAbaCommand}" CommandParameter="Ajustes">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource MenuButtonStyle}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding AbaAtiva}" Value="Ajustes">
                                                    <Setter Property="Background" Value="{StaticResource ElevatedGray2}"/>
                                                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE713;" FontSize="16" Margin="0,0,16,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="Configurações (Admin)" FontSize="15" VerticalAlignment="Center" FontWeight="Medium"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </StackPanel>

                        <!-- Perfil do usuário -->
                        <Grid Grid.Row="3">
                            <ToggleButton x:Name="BtnMenuPerfil" Background="Transparent" BorderThickness="0" Cursor="Hand"
                                          AutomationProperties.Name="Abrir menu do perfil">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="BgBorder" Background="{StaticResource ElevatedGray2}" CornerRadius="12" Padding="12">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <Border Grid.Column="0" Width="32" Height="32" CornerRadius="16" Background="#D1D1D6">
                                                    <TextBlock FontFamily="Segoe MDL2 Assets" 
                                                               Text="&#xE77B;" 
                                                               FontSize="16" 
                                                               Foreground="#6B7280" 
                                                               HorizontalAlignment="Center" 
                                                               VerticalAlignment="Center"
                                                               AutomationProperties.Name="Ícone do usuário"/>
                                                </Border>
                                                <StackPanel Grid.Column="1" Margin="10,0,0,0" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding NomeUsuario, FallbackValue='Usuário'}" Foreground="{StaticResource TextPrimary}" FontSize="13" FontWeight="SemiBold"/>
                                                    <TextBlock Text="{Binding PerfilUsuario, FallbackValue='Perfil'}" Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                                                </StackPanel>
                                                <TextBlock Grid.Column="2" FontFamily="Segoe MDL2 Assets" Text="&#xE70D;" Foreground="{StaticResource TextSecondary}" FontSize="12" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="BgBorder" Property="Opacity" Value="0.8"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="BgBorder" Property="Opacity" Value="0.6"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>

                            <Popup IsOpen="{Binding IsChecked, ElementName=BtnMenuPerfil}" 
                                   PlacementTarget="{Binding ElementName=BtnMenuPerfil}" 
                                   Placement="Top" 
                                   StaysOpen="False" 
                                   AllowsTransparency="True" 
                                   VerticalOffset="-10"
                                   HorizontalOffset="-10">
                                <Border Background="White" CornerRadius="12" BorderThickness="1" BorderBrush="#E5E5EA" Padding="16" Width="240">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="15" ShadowDepth="4" Opacity="0.1" Color="Black"/>
                                    </Border.Effect>
                                    <StackPanel>
                                        <TextBlock Text="Informações da Conta" FontSize="14" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,16"/>
                                        <TextBlock Text="Usuário Conectado" FontSize="11" Foreground="#6B7280"/>
                                        <TextBlock Text="{Binding NomeUsuario, FallbackValue='Nome do Usuário'}" FontSize="13" Foreground="#111827" FontWeight="Medium" Margin="0,2,0,12"/>
                                        <TextBlock Text="Nível de Acesso" FontSize="11" Foreground="#6B7280"/>
                                        <TextBlock Text="{Binding PerfilUsuario, FallbackValue='Administrador'}" FontSize="13" Foreground="#111827" FontWeight="Medium" Margin="0,2,0,16"/>
                                        <Separator Background="#E5E5EA" Margin="0,0,0,12"/>
                                        <Button Click="BtnAbrirModalSair_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Left">
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xF3B1;" Foreground="#EF4444" FontSize="16" VerticalAlignment="Center"/>
                                                <TextBlock Text="Encerrar Sessão" Foreground="#EF4444" FontSize="13" FontWeight="SemiBold" Margin="8,0,0,0" VerticalAlignment="Center"/>
                                            </StackPanel>
                                        </Button>
                                    </StackPanel>
                                </Border>
                            </Popup>
                        </Grid>
                    </Grid>
                </Border>

                <!-- Área de conteúdo principal com TopBar de Notificações -->
                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="Transparent" Padding="0,15,30,0">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <Button x:Name="BtnNotificacoes" Background="Transparent" BorderThickness="0" Cursor="Hand" Margin="0,0,20,0" ToolTip="Ver notificações">
                                <Grid>
                                    <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE9F5;" FontSize="24" Foreground="#6B7280" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    <Border Background="#EF4444" CornerRadius="10" Width="20" Height="20" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8,-6,0,0">
                                        <TextBlock Text="3" Foreground="White" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </Grid>
                            </Button>
                        </StackPanel>
                    </Border>

                    <ScrollViewer Grid.Row="1" Padding="40" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <ContentControl>
                            <ContentControl.Style>
                                <Style TargetType="ContentControl">
                                    <Setter Property="Content">
                                        <Setter.Value>
                                            <views:DashboardView/>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding AbaAtiva}" Value="Projetos">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <views:RastreabilidadeView/>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding AbaAtiva}" Value="Cadastro">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <views:FornecedoresView/>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding AbaAtiva}" Value="Analises">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <views:AnalisesView/>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding AbaAtiva}" Value="Pecas">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <views:PecasView/>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding AbaAtiva}" Value="Relatorios">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <views:RelatoriosView/>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding AbaAtiva}" Value="Ajustes">
                                            <Setter Property="Content">
                                                <Setter.Value>
                                                    <views:AjustesView/>
                                                </Setter.Value>
                                            </Setter>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ContentControl.Style>
                        </ContentControl>
                    </ScrollViewer>
                </Grid>
            </Grid>
        </Border>

        <!-- ========================================== -->
        <!-- Tela de Login (deslogado)                   -->
        <!-- ========================================== -->
        <Border Background="White" CornerRadius="20" BorderBrush="#E5E5EA" BorderThickness="1" Margin="10">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoggedIn}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.1"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Lado esquerdo (marca) -->
                <Border Grid.Column="0" Background="#0A5B43" CornerRadius="20,0,0,20" ClipToBounds="True">
                    <Grid>
                        <Canvas Opacity="0.8">
                            <TextBlock Text="🌿" FontSize="180" Foreground="#06402F" Opacity="0.15" Canvas.Left="-40" Canvas.Top="-20">
                                <TextBlock.RenderTransform>
                                    <RotateTransform Angle="35" CenterX="90" CenterY="90"/>
                                </TextBlock.RenderTransform>
                            </TextBlock>
                            <TextBlock Text="🌿" FontSize="250" Foreground="#06402F" Opacity="0.1" Canvas.Right="-80" Canvas.Bottom="-50">
                                <TextBlock.RenderTransform>
                                    <RotateTransform Angle="-25" CenterX="125" CenterY="125"/>
                                </TextBlock.RenderTransform>
                            </TextBlock>
                            <TextBlock Text="🌿" FontSize="120" Foreground="#1B7055" Opacity="0.2" Canvas.Left="350" Canvas.Top="80">
                                <TextBlock.RenderTransform>
                                    <RotateTransform Angle="110" CenterX="60" CenterY="60"/>
                                </TextBlock.RenderTransform>
                            </TextBlock>
                            <TextBlock Text="🌿" FontSize="90" Foreground="#1B7055" Opacity="0.25" Canvas.Left="80" Canvas.Bottom="120">
                                <TextBlock.RenderTransform>
                                    <RotateTransform Angle="-70" CenterX="45" CenterY="45"/>
                                </TextBlock.RenderTransform>
                            </TextBlock>
                        </Canvas>
                        <Grid Margin="50">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <Border Width="90" Height="90" CornerRadius="25" Background="#1B7055" HorizontalAlignment="Center" Margin="0,0,0,24">
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="25" ShadowDepth="10" Opacity="0.2"/>
                                    </Border.Effect>
                                    <TextBlock FontFamily="Segoe UI Emoji" Text="🌿" FontSize="48" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock Text="IVECO" Foreground="White" FontWeight="Black" FontSize="46" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                                <TextBlock Text="Green Ledger" Foreground="#A3D1C2" FontSize="22" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,16"/>
                                <Border Width="40" Height="3" Background="#1B7055" CornerRadius="1" HorizontalAlignment="Center"/>
                            </StackPanel>
                            <TextBlock Grid.Row="2" Text="© 2026 Iveco S.p.A." Foreground="#8CBFA3" FontSize="13" HorizontalAlignment="Center"/>
                        </Grid>
                    </Grid>
                </Border>

                <!-- Lado direito (formulário) -->
                <Grid Grid.Column="1" Background="White">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="24">
                        <Button Width="30" Height="30" Margin="0,0,4,0" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="MinimizeButton_Click" ToolTip="Minimizar">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE921;" FontSize="12" Foreground="#6B7280" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Button>
                        <Button Width="30" Height="30" Margin="0,0,4,0" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="MaximizeButton_Click" ToolTip="Maximizar">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE922;" FontSize="12" Foreground="#6B7280" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Button>
                        <Button Width="30" Height="30" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="CloseButton_Click" ToolTip="Fechar">
                            <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xE8BB;" FontSize="12" Foreground="#EF4444" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Button>
                    </StackPanel>

                    <StackPanel VerticalAlignment="Center" Margin="80,0,100,0">
                        <TextBlock Text="Acessar Plataforma" FontSize="36" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,8"/>
                        <TextBlock Text="Insira suas credenciais corporativas." FontSize="16" Foreground="#6B7280" Margin="0,0,0,45"/>

                        <TextBlock FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8">
                            <Run Text="Endereço de E-mail " Foreground="#374151"/>
                            <Run Text="*" Foreground="#EF4444"/>
                        </TextBlock>
                        <Border Background="#F9FAFB" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="10" Margin="0,0,0,24">
                            <TextBox Text="{Binding LoginEmail, UpdateSourceTrigger=PropertyChanged}" 
                                     Background="Transparent" BorderThickness="0" Padding="16,14" FontSize="15" Foreground="#111827"/>
                        </Border>

                        <TextBlock FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8">
                            <Run Text="Palavra-Passe " Foreground="#374151"/>
                            <Run Text="*" Foreground="#EF4444"/>
                        </TextBlock>
                        <Border Background="#F9FAFB" BorderBrush="#D1D5DB" BorderThickness="1" CornerRadius="10" Margin="0,0,0,40">
                            <Grid>
                                <PasswordBox x:Name="SenhaPasswordBox" Background="Transparent" BorderThickness="0" Padding="16,14,45,14" FontSize="15" Foreground="#111827" PasswordChanged="SenhaPasswordBox_PasswordChanged"/>
                                <TextBox x:Name="SenhaVisivelTextBox" Background="Transparent" BorderThickness="0" Padding="16,14,45,14" FontSize="15" Foreground="#111827" Visibility="Collapsed"/>
                                <Button Width="36" Height="36" HorizontalAlignment="Right" Margin="0,0,8,0" 
                                        Background="Transparent" BorderThickness="0" Cursor="Hand" Click="BtnMostrarSenha_Click" ToolTip="Mostrar/Ocultar Senha">
                                    <TextBlock x:Name="IconeOlho" FontFamily="Segoe MDL2 Assets" Text="&#xE18B;" FontSize="16" Foreground="#6B7280" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Button>
                            </Grid>
                        </Border>

                        <!-- Mensagem de erro de login -->
                        <TextBlock Text="{Binding LoginError}" 
                                   Foreground="Red" 
                                   FontSize="14" 
                                   FontWeight="SemiBold" 
                                   Margin="0,0,0,16" 
                                   TextWrapping="Wrap">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasLoginError}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>

                        <Button Command="{Binding FazerLoginCommand}" Background="#0A5B43" Padding="0,18" Cursor="Hand" BorderThickness="0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10">
                                        <Border.Effect>
                                            <DropShadowEffect BlurRadius="15" ShadowDepth="4" Opacity="0.3" Color="#0A5B43"/>
                                        </Border.Effect>
                                        <TextBlock FontWeight="Bold" FontSize="16" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" Text="ENTRAR"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="border" Property="Opacity" Value="0.75"/>
                                            <Setter TargetName="border" Property="Effect" Value="{x:Null}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- ========================================== -->
        <!-- MODAL DE CONFIRMAÇÃO DE SAÍDA               -->
        <!-- ========================================== -->
        <Grid x:Name="ModalConfirmacaoSair" Visibility="Collapsed" Background="#80000000" Panel.ZIndex="999">
            <Border Background="White" CornerRadius="16" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="24" Width="450">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="25" ShadowDepth="4" Opacity="0.2" Color="Black"/>
                </Border.Effect>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Grid.Column="0" Width="64" Height="64" CornerRadius="32" Background="#FEF2F2" BorderBrush="#FECACA" BorderThickness="1" VerticalAlignment="Top" Margin="0,0,16,0">
                        <TextBlock FontFamily="Segoe MDL2 Assets" Text="&#xF3B1;" FontSize="28" Foreground="#DC2626" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="4,0,0,0"/>
                    </Border>

                    <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,4,0,24">
                        <TextBlock Text="Confirmar Saída" FontSize="22" FontWeight="Bold" Foreground="#1E293B" Margin="0,0,0,8"/>
                        <TextBlock Text="Você realmente deseja encerrar sua sessão atual e voltar para a tela de login?" FontSize="15" Foreground="#64748B" TextWrapping="Wrap" LineHeight="22"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Click="BtnFecharModalSair_Click" Background="White" Cursor="Hand" Margin="0,0,12,0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="8" Padding="24,10">
                                        <TextBlock Text="Cancelar" Foreground="#475569" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>

                        <Button Click="BtnConfirmarSaida_Click" Background="#16A34A" Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="bgBorder" Background="{TemplateBinding Background}" CornerRadius="8" Padding="32,10">
                                        <TextBlock Text="Sair" Foreground="White" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bgBorder" Property="Background" Value="#15803D"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- ========================================== -->
        <!-- NOTIFICAÇÃO TOAST                            -->
        <!-- ========================================== -->
        <Border x:Name="ToastContainer" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,60" Panel.ZIndex="1000" CornerRadius="12" Padding="16,14" MaxWidth="600">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="#1E293B"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding NotificationType}" Value="Success">
                            <Setter Property="Background" Value="#10B981"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding NotificationType}" Value="Error">
                            <Setter Property="Background" Value="#EF4444"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsNotificationVisible}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Border.Effect>
                <DropShadowEffect BlurRadius="25" ShadowDepth="4" Opacity="0.25" Color="Black"/>
            </Border.Effect>

            <StackPanel Orientation="Horizontal">
                <TextBlock FontFamily="Segoe MDL2 Assets" FontSize="20" Foreground="White" Margin="0,0,12,0" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="&#xE73E;"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding NotificationType}" Value="Error">
                                    <Setter Property="Text" Value="&#xE711;"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <TextBlock Text="{Binding NotificationMessage, FallbackValue='Operação realizada com sucesso!'}" Foreground="White" FontSize="14" FontWeight="Medium" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>