<UserControl x:Class="WpfIveco.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf">

    <UserControl.Resources>
        <Style x:Key="H1" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style x:Key="Subtitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="Margin" Value="0,0,0,24"/>
        </Style>
        <Style x:Key="H2" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Margin" Value="0,0,0,24"/>
        </Style>

        <Style x:Key="PremiumCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="15" ShadowDepth="2" Opacity="0.04" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BadgeStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="6"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Style="{StaticResource H1}" Text="Visão Geral do Ledger"/>
            <TextBlock Style="{StaticResource Subtitle}" Text="Acompanhamento em tempo real da cadeia de suprimentos sustentável."/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,30">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource PremiumCardStyle}" Margin="0,0,10,0" Padding="24">
                <StackPanel>
                    <Border Width="44" Height="44" Background="#E1F0FA" HorizontalAlignment="Left" CornerRadius="12">
                        <TextBlock FontFamily="Segoe UI Emoji" Text="🚚" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Total de Veículos" Foreground="{StaticResource TextSecondary}" FontSize="14" FontWeight="Medium" Margin="0,16,0,4"/>
                    <TextBlock Text="{Binding Rastreabilidade.TotalVeiculos, FallbackValue='15'}" Foreground="{StaticResource TextPrimary}" FontSize="36" FontWeight="Bold"/>
                    <Border Style="{StaticResource BadgeStyle}" Background="#E1F0FA" Margin="0,10,0,0">
                        <TextBlock Text="Ao Vivo do Firebase" Foreground="#2980B9" FontSize="11" FontWeight="SemiBold"/>
                    </Border>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource PremiumCardStyle}" Margin="10,0,10,0" Padding="24">
                <StackPanel>
                    <Border Width="44" Height="44" CornerRadius="12" Background="#E6F4EA" HorizontalAlignment="Left">
                        <TextBlock FontFamily="Segoe UI Emoji" Text="🤝" FontSize="24" Foreground="{StaticResource AppleGreen}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Fornecedores Ativos" Foreground="{StaticResource TextSecondary}" FontSize="14" FontWeight="Medium" Margin="0,16,0,4"/>
                    <TextBlock Text="{Binding Fornecedor.TotalFornecedores, FallbackValue='0'}" Foreground="{StaticResource TextPrimary}" FontSize="36" FontWeight="Bold"/>
                    <Border Style="{StaticResource BadgeStyle}" Background="#E6F4EA" Margin="0,10,0,0">
                        <TextBlock Text="Em Conformidade" Foreground="#27AE60" FontSize="11" FontWeight="SemiBold"/>
                    </Border>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" Style="{StaticResource PremiumCardStyle}" Margin="10,0,0,0" Padding="24">
                <StackPanel>
                    <Border Width="44" Height="44" CornerRadius="12" Background="#E6F4EA" HorizontalAlignment="Left">
                        <TextBlock FontFamily="Segoe UI Emoji" Text="♻️" FontSize="24" Foreground="#27AE60" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="Pegada Média (kg CO2)" Foreground="{StaticResource TextSecondary}" FontSize="14" FontWeight="Medium" Margin="0,16,0,4"/>
                    <TextBlock Text="{Binding Dashboard.PegadaMediaFormatada, FallbackValue='590,4 kg CO2'}" Foreground="{StaticResource TextPrimary}" FontSize="36" FontWeight="Bold"/>
                    <Border Style="{StaticResource BadgeStyle}" Background="#E6F4EA" Margin="0,10,0,0">
                        <TextBlock Text="Baseado no Ledger" Foreground="#27AE60" FontSize="11" FontWeight="SemiBold"/>
                    </Border>
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource PremiumCardStyle}" Padding="24" MinHeight="350">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Emissões da Cadeia" Style="{StaticResource H2}"/>

                <lvc:CartesianChart Grid.Row="1" 
                                    Series="{Binding Analises.EmissoesSeries}" 
                                    Margin="0,10,0,0" 
                                    LegendLocation="Top">

                    <lvc:CartesianChart.AxisX>
                        <lvc:Axis Labels="{Binding Analises.MesesLabels}" 
                                  Foreground="{StaticResource TextSecondary}"
                                  FontSize="12">
                            <lvc:Axis.Separator>
                                <lvc:Separator Step="1" StrokeThickness="0"/>
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisX>

                    <lvc:CartesianChart.AxisY>
                        <lvc:Axis Title="Ton CO₂" 
                                  Foreground="{StaticResource TextSecondary}"
                                  FontSize="12"
                                  LabelFormatter="{Binding Analises.Formatter}"
                                  MinValue="0">
                            <lvc:Axis.Separator>
                                <lvc:Separator Stroke="{StaticResource ElevatedGray3}" 
                                               StrokeDashArray="4" 
                                               StrokeThickness="1"/>
                            </lvc:Axis.Separator>
                        </lvc:Axis>
                    </lvc:CartesianChart.AxisY>
                </lvc:CartesianChart>
            </Grid>
        </Border>
    </Grid>
</UserControl>