<UserControl x:Class="WpfIveco.Views.AnalisesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TÍTULO -->
        <StackPanel Grid.Row="0">
            <TextBlock Style="{StaticResource H1}" Text="Análise de Sustentabilidade"/>
            <TextBlock Style="{StaticResource Subtitle}" Text="Métricas detalhadas para emissão de relatórios corporativos ESG."/>
        </StackPanel>

        <!-- CONTEÚDO -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- GRÁFICO DE PIZZA -->
            <Border Grid.Column="0" Style="{StaticResource PremiumCardStyle}" Margin="0,0,10,0" Padding="30" MinHeight="400">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Distribuição de Emissões" Style="{StaticResource H2}"/>
                    <lvc:PieChart Grid.Row="1" 
                                  Series="{Binding Analises.DistribuicaoSeries}" 
                                  Margin="0,10,0,0" 
                                  LegendLocation="Bottom"
                                  Foreground="{StaticResource TextPrimary}">
                        <lvc:PieChart.DataTooltip>
                            <lvc:DefaultTooltip Background="White" BorderBrush="#E5E5EA" BorderThickness="1" CornerRadius="8"/>
                        </lvc:PieChart.DataTooltip>
                    </lvc:PieChart>
                </Grid>
            </Border>

            <!-- TOP FORNECEDORES VERDES -->
            <Border Grid.Column="1" Style="{StaticResource PremiumCardStyle}" Margin="10,0,0,0" Padding="30" MinHeight="400">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Top Fornecedores Verdes" Style="{StaticResource H2}"/>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Analises.TopFornecedores}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="{StaticResource ElevatedGray3}" BorderThickness="0,0,0,1" Padding="0,0,0,12" Margin="0,0,0,12">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- CÍRCULO COM COR VIA DATATRIGGERS (SEM CONVERSOR) -->
                                            <Border Grid.Column="0" Width="36" Height="36" CornerRadius="18" Margin="0,0,12,0"
                                                    HorizontalAlignment="Center" VerticalAlignment="Center">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#FF9500"/>
                                                        <!-- Laranja padrão -->
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Certificado}" Value="ISO 14001">
                                                                <Setter Property="Background" Value="#2E7D32"/>
                                                                <!-- Verde -->
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Certificado}" Value="Pendente">
                                                                <Setter Property="Background" Value="#FF9500"/>
                                                                <!-- Laranja -->
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="✓" FontSize="16" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>

                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Nome}" Foreground="{StaticResource TextPrimary}" FontWeight="SemiBold" FontSize="14"/>
                                                <TextBlock Text="{Binding Localizacao}" Foreground="{StaticResource TextSecondary}" FontSize="12"/>
                                                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                                    <TextBlock Text="Peças: " Foreground="{StaticResource TextSecondary}" FontSize="11"/>
                                                    <TextBlock Text="{Binding TotalPecas}" Foreground="{StaticResource AppleBlue}" FontSize="11" FontWeight="Bold"/>
                                                    <TextBlock Text=" | Pegada: " Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="8,0,0,0"/>
                                                    <TextBlock Text="{Binding PegadaMedia, StringFormat={}{0:N1} kg CO2}" Foreground="{StaticResource AppleOrange}" FontSize="11" FontWeight="Bold"/>
                                                </StackPanel>
                                            </StackPanel>

                                            <Border Grid.Column="2" Background="#E6F0FF" CornerRadius="4" Padding="8,4" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Certificado}" Foreground="{StaticResource AppleBlue}" FontSize="10" FontWeight="Bold"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>